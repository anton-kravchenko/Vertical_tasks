#include <stdio.h>
#include <stdlib.h>

void WriteLn(int);

int main(void) {


	int a, b, c;
	a = b + c
	;

	;WriteLn(a);
	// Все компилится все собирается ошибок нет
	// На одной из стадий синтаксического разбора компилятор удаляет незначимые пробелы и символы переноса строки



	struct {
			int a;
		} s;
		s 		.			 a;;;;;;;;

	// Поэтому компилируется даже такое
	return EXIT_SUCCESS;
}

void WriteLn(int a)
{
	printf("%d", a);
}
